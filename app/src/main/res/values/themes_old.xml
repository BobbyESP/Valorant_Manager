<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorIntroApp</item>
        <item name="colorPrimaryDark">@color/colorIntroApp</item>
        <item name="android:colorEdgeEffect">@color/redValorant</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="MainTheme" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/redValorant</item>
        <item name="colorPrimaryDark">@color/redValorantDarker2</item>
        <item name="android:colorEdgeEffect">@color/redValorant</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SplashScreen" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splashscreen</item>
    </style>

    <style name="LoginBoxes" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/redValorant</item>
        <item name="boxStrokeWidth">1dp</item>
        <item name="android:textColorHint">@color/redValorant</item>
        <item name="boxStrokeErrorColor">@color/redValorant</item>
    </style>
</resources>